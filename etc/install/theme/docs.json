{
	"_design/auth": {
		"language": "javascript",
		"validate_doc_update": "function (newDoc, oldDoc, userCtx) {
			if (userCtx.roles.indexOf('_admin') >= 0 || userCtx.roles.indexOf('ccms-admin') >= 0) {
				return;
			} else {
				throw({ unauthorized : 'You need to be CCMS or server admin to edit this database.' });
			}
		}"
	},
	"_design/posts": {
		"language": "javascript",
		"views": {
			"byDate": {
				"map": "function (doc) { if (doc.type == 'post') emit([doc.date, doc.postID], doc); }"
			},
			"compactByDate": {
				"map": "function (doc) { if (doc.type == 'post') emit([doc.date, doc.postID]); }"
			},
			"byPostID": {
				"map": "function (doc) { if (doc.type == 'post') emit(doc.postID, doc); }"
			}
		}
	},
	"meta": {
		"system": null,
		"copyright": null,
		"description": "",
		"postsPerPage": 10,
		"theme": "default",
		"title": null
	},
	"test": {
		"time": 0
	}
}